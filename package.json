{
  "private": true,
  "scripts": {
    "dev": "pnpm --filter vectors-app dev",
    "build": "pnpm --filter vectors-app build",
    "start": "pnpm --filter vectors-app start",
    "lint": "pnpm --filter vectors-app lint"
  }
}
